browser=chrome
headless=true
implicit.wait.seconds=0
explicit.wait.seconds=10

# H2 is used automatically for runnable local Cucumber tests.
db.url=jdbc:h2:mem:banking;MODE=MSSQLServer;DB_CLOSE_DELAY=-1
db.username=sa
db.password=
